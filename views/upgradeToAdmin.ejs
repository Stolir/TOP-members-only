<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/global.css">
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>
    <form action="/upgrade-account/admin" method="post">
      <div>
        <label for="adminPassword">Admin Password</label>
        <input
          type="password"
          name="adminPassword"
          id="adminPassword"
          required
        />
        <% if (errors?.adminPassword) { %>
          <span class="error"><%= errors.adminPassword %></span>
        <% } %>
      </div>
      <button type="submit">Submit</button>
    </form>
  </body>
</html>
